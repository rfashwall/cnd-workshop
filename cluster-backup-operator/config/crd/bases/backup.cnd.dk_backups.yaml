---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: backups.backup.cnd.dk
spec:
  group: backup.cnd.dk
  names:
    kind: Backup
    listKind: BackupList
    plural: backups
    singular: backup
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: Backup is the Schema for the backups API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: BackupSpec defines the desired state of Backup.
            properties:
              retention:
                description: Retention defines how long to keep backups
                type: string
              schedule:
                description: Schedule defines when to perform backups (cron format)
                type: string
              source:
                description: Source defines what to backup (e.g., namespace, deployment)
                properties:
                  labelSelector:
                    description: LabelSelector for resources to backup
                    properties:
                      matchExpressions:
                        description: matchExpressions is a list of label selector
                          requirements. The requirements are ANDed.
                        items:
                          description: |-
                            A label selector requirement is a selector that contains values, a key, and an operator that
                            relates the key and values.
                          properties:
                            key:
                              description: key is the label key that the selector
                                applies to.
                              type: string
                            operator:
                              description: |-
                                operator represents a key's relationship to a set of values.
                                Valid operators are In, NotIn, Exists and DoesNotExist.
                              type: string
                            values:
                              description: |-
                                values is an array of string values. If the operator is In or NotIn,
                                the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                the values array must be empty. This array is replaced during a strategic
                                merge patch.
                              items:
                                type: string
                              type: array
                              x-kubernetes-list-type: atomic
                          required:
                          - key
                          - operator
                          type: object
                        type: array
                        x-kubernetes-list-type: atomic
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: |-
                          matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                          map is equivalent to an element of matchExpressions, whose key field is "key", the
                          operator is "In", and the values array contains only "value". The requirements are ANDed.
                        type: object
                    type: object
                    x-kubernetes-map-type: atomic
                  namespace:
                    description: Namespace to backup
                    type: string
                required:
                - namespace
                type: object
              storageLocation:
                description: StorageLocation defines where to store backups
                properties:
                  bucket:
                    description: Bucket name
                    type: string
                  credentialsSecret:
                    description: Credentials secret reference
                    type: string
                  endpoint:
                    description: Endpoint URL (for Minio)
                    type: string
                  provider:
                    description: Provider (e.g., "minio", "s3", "gcs")
                    type: string
                required:
                - bucket
                - provider
                type: object
            required:
            - schedule
            - source
            - storageLocation
            type: object
          status:
            description: BackupStatus defines the observed state of Backup.
            properties:
              backupCount:
                description: BackupCount is the total number of backups performed
                format: int32
                type: integer
              lastBackupTime:
                description: LastBackupTime is the timestamp of the last successful
                  backup
                format: date-time
                type: string
              message:
                description: Message provides additional information about the current
                  state
                type: string
              nextBackupTime:
                description: NextBackupTime is the timestamp of the next scheduled
                  backup
                format: date-time
                type: string
              phase:
                description: Phase represents the current phase of the backup
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
